# =========================================================
# CONFIGURAÇÕES DO DJANGO
# =========================================================

# Chave secreta do Django (obrigatório em produção)
# Gere uma nova chave: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=django-insecure-ig95=xwa#v5&1j+5b9qq^v1bjnkuq98m!(*jf)o6(l&9=k&6gv

# Modo debug (True/False ou 1/0)
DJANGO_DEBUG=True

# Hosts permitidos (separados por vírgula)
# Em produção, defina os domínios específicos: exemplo.com,api.exemplo.com
# Em desenvolvimento com DEBUG=True, pode usar *
DJANGO_ALLOWED_HOSTS=*

# Nome do banco de dados SQLite (opcional, padrão: db.sqlite3)
# Usado apenas quando PostgreSQL não está configurado
DJANGO_DB_NAME=db.sqlite3

# URL base para arquivos estáticos (opcional, padrão: static/)
DJANGO_STATIC_URL=static/


# =========================================================
# CONFIGURAÇÕES DO BANCO DE DADOS - POSTGRESQL (Docker/Produção)
# =========================================================

# Configuração PostgreSQL (opcional - se não definido, usa SQLite)
# Se todas as variáveis abaixo estiverem definidas, o sistema usa PostgreSQL
# Caso contrário, usa SQLite automaticamente (desenvolvimento)
# 
# Para usar PostgreSQL, descomente e configure as variáveis abaixo:

# Nome do banco de dados PostgreSQL
# POSTGRES_DB=glpi_auto_ticket

# Usuário do PostgreSQL
# POSTGRES_USER=postgres

# Senha do PostgreSQL
# POSTGRES_PASSWORD=sua_senha_segura_aqui

# Host do PostgreSQL (padrão: db no Docker, localhost fora)
# No Docker Compose, use o nome do serviço: db
# POSTGRES_HOST=db

# Porta do PostgreSQL (padrão: 5432)
# POSTGRES_PORT=5432


# =========================================================
# CONFIGURAÇÕES DO SUPERUSER (Docker)
# =========================================================

# Criação automática de superuser no Docker (opcional)
# Se definido, cria automaticamente um superuser ao iniciar o container

# Nome de usuário do superuser
DJANGO_SUPERUSER_USERNAME=admin

# Senha do superuser
DJANGO_SUPERUSER_PASSWORD=admin123

# Email do superuser (opcional, padrão: admin@example.com)
DJANGO_SUPERUSER_EMAIL=admin@example.com


# =========================================================
# CONFIGURAÇÕES DO GLPI - API V2.1 (Futuro)
# =========================================================

# URL base da API do GLPI v2.1 (opcional, para uso futuro)
GLPI_API_URL=

# Token de autenticação da API v2.1 (opcional, para uso futuro)
GLPI_API_TOKEN=

# URL base do GLPI (opcional, padrão: http://localhost)
GLPI_BASE_URL=http://localhost


# =========================================================
# CONFIGURAÇÕES DO GLPI - API LEGACY (Sincronização de Categorias)
# =========================================================

# URL da API Legacy do GLPI (obrigatório para sincronização de categorias)
# O código adiciona automaticamente /api.php/v1 se não estiver presente
# Exemplos válidos:
#   http://seu-servidor-glpi:porta (será formatado para http://seu-servidor-glpi:porta/api.php/v1)
#   http://seu-servidor-glpi:porta/api.php/v1 (mantém como está)
GLPI_LEGACY_API_URL=http://seu-servidor-glpi:porta

# Usuário para autenticação na API Legacy
GLPI_LEGACY_API_USER=seu_usuario_glpi

# Senha do usuário para autenticação na API Legacy
GLPI_LEGACY_API_PASSWORD=sua_senha_aqui

# Token do aplicativo (opcional, se configurado no GLPI)
GLPI_LEGACY_APP_TOKEN=


# =========================================================
# CONFIGURAÇÕES DO N8N
# =========================================================

# URL do webhook n8n para atualizar pesquisa de satisfação no GLPI
# Formato: http://seu-n8n:5678/webhook/glpi/survey-response
# Opcional: apenas necessário se quiser sincronizar pesquisas de satisfação com GLPI
N8N_SURVEY_RESPONSE_WEBHOOK_URL=

# URL do webhook n8n para aprovar/rejeitar sugestões de categoria no GLPI
# Formato: http://seu-n8n:5678/webhook/glpi/category-approval
# Obrigatório para usar os endpoints de aprovação/rejeição (o backend espera 2xx do n8n)
N8N_CATEGORY_APPROVAL_WEBHOOK_URL=


# =========================================================
# CONFIGURAÇÕES DO GOOGLE GEMINI AI
# =========================================================

# Chave da API do Google Gemini para classificação automática de tickets
# Obtenha em: https://makersuite.google.com/app/apikey
# Opcional: sistema funciona sem IA, mas não retornará classificações
GEMINI_API_KEY=


# =========================================================
# CONFIGURAÇÕES DO ZOHO API (SSPR)
# =========================================================

# Client ID do Zoho (obtido ao criar app no Zoho Developer Console)
# https://api-console.zoho.com/
ZOHO_CLIENT_ID=

# Client Secret do Zoho (obtido ao criar app no Zoho Developer Console)
ZOHO_CLIENT_SECRET=

# Refresh Token do Zoho (gerado UMA VEZ após autorização OAuth)
# IMPORTANTE: Este token não expira e é usado para renovar access tokens automaticamente
# Para gerar:
# 1. Acesse: https://accounts.zoho.com/oauth/v2/auth?scope=ZohoMail.organization.READ,ZohoMail.organization.accounts.ALL&client_id=SEU_CLIENT_ID&response_type=code&access_type=offline&redirect_uri=https://localhost/callback
# 2. Copie o 'code' retornado
# 3. Faça POST para https://accounts.zoho.com/oauth/v2/token com grant_type=authorization_code
# 4. Copie o 'refresh_token' da resposta
# 5. Salve aqui - será criado automaticamente no banco na primeira vez que ZohoClient for usado
# IMPORTANTE: O scope é definido quando você gera o refresh_token no OAuth.
# Para reset de senha, use scope: ZohoMail.organization.accounts.ALL ou ZohoMail.organization.accounts.UPDATE
ZOHO_REFRESH_TOKEN=

# Organization ID (zoid) do Zoho (recomendado configurar estaticamente)
# Contas comuns não conseguem usar o endpoint GET /api/organization
# Obtenha o zoid via admin do Zoho ou consulte sua organização
ZOHO_ORGANIZATION_ID=873090368

# API Domain do Zoho (opcional, padrão: https://www.zohoapis.com)
ZOHO_API_DOMAIN=https://www.zohoapis.com


# =========================================================
# CONFIGURAÇÕES DO TWILIO (SSPR - SMS OTP)
# =========================================================

# Account SID do Twilio (obtido no Dashboard do Twilio)
# https://console.twilio.com/
TWILIO_ACCOUNT_SID=

# Auth Token do Twilio (obtido no Dashboard do Twilio)
# https://console.twilio.com/
TWILIO_AUTH_TOKEN=

# Número de telefone Twilio para envio de SMS (formato: +5511999999999)
# Obtido ao criar um número de telefone no Twilio
# https://console.twilio.com/us1/develop/phone-numbers/manage/incoming
TWILIO_PHONE_NUMBER=